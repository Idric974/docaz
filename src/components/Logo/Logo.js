import React from 'react';
import styles from './Logo.module.css';

const Thread = () => {
  return (
    <div className={styles.box}>
      <div className={styles.logoText}>docaz.re</div>
    </div>
  );
};

export default Thread;
